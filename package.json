{
    "name": "native-nyar",
    "private": true,
    "scripts": {
        "prebuild": "cargo run --release --color=always --package nyar_prebuild --bin nyar_prebuild && cargo fmt --all",
        "build": "cargo build --release",
        "test": "cargo build && cargo fmt --all",
        "push": "yarn test && git push",
        "reset": "git reset Head~ --soft"
    }
}
