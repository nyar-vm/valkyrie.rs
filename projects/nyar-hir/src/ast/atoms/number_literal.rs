use super::*;

#[derive(Debug, Clone)]
pub struct NumberLiteral {
    pub handler: Option<String>,
    pub value: String,
}
// pub struct NumberLiteral {
//     handler: Option<StringRange>,
//     value: StringRange,
// }
