namespace core.text;




structure Point {
    x: f32,
    y: f32,
    construct(x: f32, y: f32) {
        self.x = x;
        self.y = y;
    }
}

extends Point {
    sum(self): f32 {
        add(self.x, self.y)
    }
}


function add(i: f32, j: f32): f32 {
    i + j
}
