ffi("wasi:cli/environment", "get-environment")
micro get_environment(

ffi("wasi:cli/environment", "get-arguments")
micro get_arguments(

ffi("wasi:cli/environment", "initial-cwd")
micro initial_cwd(

ffi("wasi:cli/exit", "exit")
micro exit(

ffi("wasi:cli/run", "run")
micro run(

Type: Some("input-stream") Id(Id { idx: 10 })

ffi("wasi:cli/stdin", "get-stdin")
micro get_stdin(

Type: Some("output-stream") Id(Id { idx: 11 })

ffi("wasi:cli/stdout", "get-stdout")
micro get_stdout(

Type: Some("output-stream") Id(Id { idx: 11 })

ffi("wasi:cli/stderr", "get-stderr")
micro get_stderr(

#resource
#ffi("TerminalInput")
class TerminalInput {}

#resource
#ffi("TerminalOutput")
class TerminalOutput {}

Type: Some("terminal-input") Id(Id { idx: 228 })

ffi("wasi:cli/terminal-stdin", "get-terminal-stdin")
micro get_terminal_stdin(

Type: Some("terminal-output") Id(Id { idx: 229 })

ffi("wasi:cli/terminal-stdout", "get-terminal-stdout")
micro get_terminal_stdout(

Type: Some("terminal-output") Id(Id { idx: 229 })

ffi("wasi:cli/terminal-stderr", "get-terminal-stderr")
micro get_terminal_stderr(

