


## For statement

```markdown
for `pattern` in `iterator` {
    `[body]`
}
else {
    `[otherwise]`
}
```

## Loop statement

```markdown
loop {
    `[body]`
}
```

## While statement

```markdown
while `condition` {
    `[body]`
}
else {
    `[otherwise]`
}
```