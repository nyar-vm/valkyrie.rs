a::b∷c;

a + b - c;
a * b / c;
a ^ b ^ c;

5 ^ (4 * (3 + 2) - 1);
1 + 2 * 3 ^ (4 - 5);


// divide
a /% 2

%%4

// Equality
a == c;
a ≠ b != c;
a ≡ b === c;
a ≢ b !== c =!= c;
a =!= b =!= c;

// Ordering
a > b > c;
a >= b + c >= c + e >= d;
a < b < c;
a <= b <= c <= d;

// append
a ≫ b >> c >>= d;
a ⋙ b >>> c;
a ≪ b << c;
a ⋘ b <<< c <<= d;

// contains
a ∈ list;
a ∊ list;
a ∉ list;
a in list;
a not in list;

// subtyping
a <: String;
a is String;
a <! String;
b is not String;

// logic
a && b || c ∨ d ⊽ e ⊻ f ∧ g ⊼ h ⩟ i;

